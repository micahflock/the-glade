# Deferred task — finish the citation style migration

Three items in `Open_Questions.md` (the Bloom/all-magic-users, bug-scale conversion,
and red-queen/black-queen entries) now cite evidence by section heading instead of
line number. The remaining ~15 resolved items still use line numbers, which rot
whenever a source file is edited — that rot is exactly what the 2026-07-25 janitor
pass turned up.

## Prompt to run later

> Convert every remaining line-number citation in memory/Open_Questions.md to a
> section-heading citation, matching the style already used by the "Does the Bloom
> affect all magic users", "Bug-scale conversion", and "Red queen / black queen
> power dynamic" entries — e.g. `Evidence: World_Codex.md § Fire Ants` or
> `Evidence: World_Codex.md §§ Leafcutter Ants, Alliance Timeline`.
>
> For each one, open the cited file and verify the line range actually supports the
> decision before rewriting it. Several existing citations are offset or point at
> unrelated text, so treat this as a verification pass, not a mechanical reformat.
> Report any citation whose evidence does not hold up rather than silently
> retargeting it.
>
> Leave `sessions/final/*.md` citations as bare filenames — those files are
> immutable, so they do not rot. Same for `party/*.yaml` and dated DM rulings.
